computeTransition(Pattern,Alphabet)
{
	m=P.size()
	n=A.size()
	dictionary delta;
	for q in range 0 to m
		for a in range 0 to n
			k=min(m+1,q+2)
			while  p(k) not a suffix p(q)a
			k--;
			delta(q,a)=k; //key value pair index alphabet[a]
	return delta;
			
}
